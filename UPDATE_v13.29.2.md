# BXH ARENA v13.29.2 更新說明

## 本版定位

v13.29.2 是玩家介面崩潰的緊急穩定修正版。本版修正稱號／每日簽到服務讀取失敗後的無限重試，不改動登入、報名、賽事報到、裁判分桌、計分、晉級、天梯或封存流程。

## 崩潰根因與修正

- v13.29.1 在 Functions 尚未部署、權限拒絕、逾時或網路失敗時，會設定錯誤狀態並呼叫 `render()`。
- 玩家首頁重新繪製每日簽到卡時，又立即排入下一次自動讀取；每次失敗都再次重繪與重試，形成無限循環。
- 會員資料的稱號中心與最高管理後台存在相同風險。
- v13.29.2 只允許首次自動讀取一次；失敗後停止自動重試並顯示「重新連線」，由使用者主動觸發。
- 新增請求世代與 UID 驗證，登出或切換帳號後，舊請求不得寫回目前畫面。

## 已完成

- 每日簽到交易成功後，即使個別稱號判定暫時失敗，仍正確顯示「簽到成功」，避免使用者重複操作。
- 正式賽事統計觸發重試時，會再次執行可重複安全執行的稱號判定，避免暫時性錯誤造成永久漏發。
- 單一稱號判定失敗不再中止其餘稱號檢查，並保留後端錯誤資訊供追查。
- 人工授予前驗證玩家 UID、稱號 ID、帳號存在及啟用狀態，避免建立孤兒資料。
- 封測帳號的人工測試稱號標記 `isTestData`，不占正式限定稱號配額及正式領取人數。
- Callable Functions 統一設定 30 秒逾時與登入、權限、未部署、網路及逾時錯誤提示。
- Functions 部署前自動執行 JavaScript 語法檢查。
- 修補 Functions 相依套件弱點；本地 `npm audit --omit=dev` 為 0 項。
- 保留 `maxInstances: 5` 且不設定 `minInstances`，延續封測成本防護。

## 功能開關建議

首次上線先使用：

```json
{
  "titlesEnabled": true,
  "automaticTitleAwardsEnabled": false,
  "dailyCheckInEnabled": true,
  "checkInTitleAwardsEnabled": false,
  "timezone": "Asia/Taipei"
}
```

確認玩家與 tester 簽到、人工授予、裝備、隱藏及四種姓名格式都正常後，再逐步開啟自動發放與簽到稱號。

## 尚未啟用

- 開拓者前 100 名正式配額
- 舊賽事歷史補算
- 16 強、8 強、連勝、不敗、跨場地及賽季王者自動判定
- 稱號全面顯示於所有公開對戰畫面

上述項目維持停用，不會在本版被誤發。

## 本地驗證結果

- HTML 內嵌 JavaScript 語法：通過
- Functions JavaScript 語法：通過
- v13.29.2 回歸測試：通過
- 玩家端服務失敗無限重繪重現測試：通過
- Functions production dependency audit：0 項弱點
- Firestore Rules 靜態路徑及可信寫入保護檢查：通過
- Firestore Emulator：執行元件下載受目前環境網路限制，仍需在部署端執行正式 Emulator 測試
